<label for="{{ selectName }}">{{ selectLabel }}</label>
<select name="select" name="{{ selectName }}">
{% for option in selectOptions %}
    <option value="{{ option.value }}">{{ option.text }}</option>
{% endfor %}
</select>